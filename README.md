Hardware: ESP32-C3 SuperMini.

Interface: Open-Drain GPIO triggers (Pins 0, 1, 3) for contact closure.

Features: * Responsive Mobile-first Web Portal for initial setup.

    Wi-Fi Station/AP dual-mode logic.

    Non-blocking MQTT event handling for vacuum commands (START, STOP, HOME).

State Detection: (Mention your planned implementation for sensing if the vacuum is running).
